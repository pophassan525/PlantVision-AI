# PlantVision AI — dependencies
# Only packages actually used by the current code are listed as active.

# Core
numpy
pandas
pyyaml
python-dotenv

# Image handling
Pillow
opencv-python

# App
streamlit

# Deep learning
torch
torchvision

# Evaluation
scikit-learn
matplotlib
seaborn

# Will be added in later phases:
# ultralytics                   (Phase 4 — YOLO pest detection)
# grad-cam                      (Phase 11 — explainability)
# chromadb / faiss-cpu          (Phase 9 — RAG vector store)
# anthropic                     (Phase 9 — RAG LLM calls)